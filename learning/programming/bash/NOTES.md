# Bash Notes

Working notes for the teaching session. Not linked from `README.md`.

## Preferences

- Interview recorded in #41. The outcome was not narrowed: both CI and deployment scripts and operational tooling.
- **POSIX `sh` portability over bash-only idioms, asked for explicitly, even though bash is the topic's home.** A bash-only feature is called out as such when used. This inverts the obvious default for a workspace named bash, so it is the note most likely to be undone by a session that has not read it.
- `awk` and `sed` were kept in where a script genuinely needs them, rather than declared out of scope, but not as topics of their own.
- Disclosed background: no prior shell scripting experience.

## On the arc

The six stages and their seven lessons are public, in `README.md`. What belongs here is the caveat on them: the arc was written upfront rather than one lesson per interactive session, and `learning-records/` is empty. So nothing in this workspace has been calibrated against a demonstrated answer, and every practice item's difficulty is guessed.

This is the shortest arc in the repository: seven lessons over six stages, one lesson per stage except stage 2. It is complete against its own arc table, so the brevity is the shape of the mission rather than a gap to fill. A later session should not pad it out to match the language workspaces.

## Open threads

- Nothing has been attempted yet. The first interactive session should calibrate before teaching forward: take a practice item from an early lesson and check whether the level is right, rather than assuming the written arc landed.
