# Java Notes

Working notes for the teaching session. Not linked from `README.md`.

## Preferences

- **Lessons are written for a general reader, deliberately.** No machine, OS, editor or installed version appears in any lesson, reference sheet, `README.md` or `RESOURCES.md`. Requested explicitly; do not personalise the lesson material to whoever is running the session.
- Mission is the full arc: zero to senior specialist, assuming no prior Java. Start at stage 1 rather than calibrating against a particular reader.
- Reps must fit one sitting, with stage 6 excepted because profiling needs a program that runs long enough to profile.

## State

**The arc is complete.** All seven stages are written: lessons 0001 to 0049, plus seven reference sheets, `reference/equality-hashing-and-ordering.md`, `reference/modelling.md`, `reference/idiom-and-library.md`, `reference/concurrency.md`, `reference/testing-and-build.md`, `reference/the-runtime.md` and `reference/judgment.md`. Eighty glossary terms. Every stage's done-when criterion is discharged by a lesson rather than described, and lesson 0049 closes the mission's last clause, being trusted to make the call and explain it to someone else.

What that means for future work here. There is no stage 8, and the honest way to grow this workspace is not to add stages but to revise: the version policy below names what goes stale, and the "What execution changed" section names the specific findings a reviser is most likely to undo by accident. The two lessons most likely to need revision first are 0027, on virtual threads, and 0037, where compact object headers become the default in JDK 27 and the lesson's off-by-default fact stops being true. Neither is wrong today.

Stage 5 is seven lessons and covers two subjects that are usually taught apart, testing (0029 to 0032) and the build (0033 to 0035). Keeping them in one stage is deliberate, because the stage's done-when criterion is a single sentence that needs both: someone else can clone, build, test and run it. Lesson 0035 discharges that criterion literally rather than describing it, and the handover test at the end of it was performed, with the system build tool removed from the path, not merely written down.

**The tool decision is made: Maven, version 3.9 or later.** This closes the thread that had been open since the arc was planned, and it was settled on evidence rather than taste, since Maven 4 existed only as a release candidate when the stage was written. `README.md` keeps tool comparison out of scope, so the lessons teach the tasks a build performs and use Maven to perform them. If Maven 4 reaches a stable release, the lessons naming 3.9 are the ones to revisit, and lesson 0034 is where the version is discussed.

Lesson 0032 is the one to protect in this stage, and lesson 0035 is the other. 0032 because it is the stage's judgment lesson and the arc explicitly asks for "when not to use one": it demonstrates a test suite that passes while exercising nothing, and it shows a behaviour-preserving refactor breaking a mock-based test while the fake-based test on the same code keeps passing. 0035 because the dependency cliff it reproduces, a green build with passing tests and an artifact that dies on `NoClassDefFoundError`, is the failure that separates a project someone can run from one they cannot.

Stage 4 was written under two rules the earlier stages did not need: reproduce every race and say how many attempts it took, and report honestly when a race does not reproduce. Both earned their place. Three of the stage's demonstrations came out other than expected because of them, and the stage is better for saying so than it would have been for asserting the folklore.

Lesson 0023 is the stage's anchor and the piece to protect. It contains a **directly observed reordering**, which most writing on this subject says you should not expect to see: a store-buffering test produced an outcome impossible under sequential consistency on ninety-five to ninety-eight per cent of five million rounds, and marking the two fields `volatile` took it to zero on the same five million. That single pair of runs does more for the reader than the whole vocabulary does, and the lesson is built around it.

Lesson 0027 is the other one to protect, for the opposite reason: it is the lesson most likely to go stale, and it is written so that the reader is told to measure rather than to trust it.

Stage 3 is seven lessons and is organised as two halves that share one question, which is whether a reviewer would call the code unidiomatic. The first half is control flow and absence, meaning exceptions (0015) and `Optional` (0016), and the second is the library a reviewer expects you to know, meaning streams and collectors (0017, 0018) then files, time and text (0019 to 0021). Streams are split from collectors deliberately: the pipeline and the container it becomes fail in different ways, and putting them together produced a lesson where the traps drowned the mechanics.

Lesson 0018 is the one to protect in this stage, because it carries the least guessable fact in the arc: `Stream.toList` accepts a null element and `Collectors.toUnmodifiableList` refuses one, which is the opposite of what the names suggest. That asymmetry is a three-column table in the sheet for exactly this reason.

Stage 2 is eight lessons rather than the six or seven the other stages will need, because the modelling surface is genuinely wider here: a class, a record, an interface, an abstract class, a sealed hierarchy and an enum are six ways to say almost the same thing, and the stage's whole job is telling them apart. The order is the load-bearing part. Interfaces (0009) come before inheritance (0010) so that a reader meets the contract before the mechanism, and sealed types (0011) come after both because sealing is a restriction on inheritance that only makes sense once inheritance has been priced. If the stage is ever reordered, that run is what must survive.

Lesson 0010 is the one to protect. It is the only place in the arc that argues against a language feature at length, and it does that with measured evidence rather than taste: construction order producing a `null` field, static and field hiding resolving on the declared type, and the counting-set failure with real numbers before and after the fix.

**Onboarding is handled inside lesson 0007**, in a short "Running these examples" subsection, rather than by inserting a new lesson at the front. Stage 1 shipped without ever showing how to compile and run, which is a real gap for a workspace whose constraints say "assumes no prior Java", and 0007 is the first lesson where the reader declares a class of their own, so it is the honest place for it. Renumbering stage 1 to put it first would have broken every existing cross-reference for no pedagogical gain. If stage 3 shows a gap of the same kind, solve it the same way.

**How the glossary is populated here.** The skill's test, that a term lands once it can be used correctly, is a statement about a learner's demonstration. These lessons have no single learner, so the test is applied to the material: a term lands when a lesson has taught it well enough for a reader to use it. Stage 1 added seven terms alongside the three pinned ones, stage 2 added thirteen, stage 3 added eight, stage 4 added eleven, stage 5 added seventeen, stage 6 added fourteen, and stage 7 added seven for a running total of eighty. Stage 5's count is the largest and is justified rather than loose: it covers two subjects, and six of the seventeen are the five kinds of test double plus the umbrella term, which have to land together or not at all, since the whole point of lesson 0032 is that they are used as synonyms and are not. One candidate was deliberately refused: the lesson coins "dependency cliff" for the green-build-broken-artifact failure, and a coined phrase does not belong in a glossary of canonical terms, because a reader would carry it into a review where nobody else knows it. Stage 6 refused one on the same grounds, "measurement floor", which lesson 41 uses well as prose for the level below which two benchmark scores cannot be compared, but which is this workspace's phrase rather than the harness's or the platform's. Keep doing this per stage, and do not add a term the lessons have not earned. Two of stage 2's entries are deliberately a pair: "Invariance (of generics)" is written to be read against the existing "Covariance (of arrays)", since the two rules are opposites and the contrast is the lesson.

## What execution changed

Every behavioural claim in stage 2 was run rather than recalled, on the release the workspace names. Four things came out differently from what recall would have produced, and all four changed lesson content.

- **`this(...)` and `super(...)` no longer have to be the first statement.** JEP 513, Flexible Constructor Bodies, was finalised in Java 25: statements that do not touch the instance may run before the call, which is how an argument gets validated or computed before the superclass sees it. The old rule is stated in every book and was nearly written into two lessons. Verified by running a constructor with a validating prologue.
- **The classic counting-set bug does not reproduce in its usual form.** Overriding only `add` on a `HashSet` subclass gives the correct count, because `AbstractCollection.addAll` calls the overridable `add` and increments nothing itself. The double count needs both methods overridden and both incrementing. Building it on `ArrayList` never reproduces at all, since its `addAll` is a bulk copy. Lesson 0010 uses the version that actually fails.
- **Adding an enum constant without recompiling a switch throws `MatchException`**, not the `IncompatibleClassChangeError` usually named. Reproduced by split compilation, and stated as observed.
- **`List.copyOf` does not simply skip the copy for an already-unmodifiable list.** It returns the same instance for a list its own factories produced, and a new one for a `Collections.unmodifiableList` view, because the view is an implementation it cannot recognise as safe. Lesson 0014 states that distinction rather than the flat claim.

A fifth detail worth keeping: `this.field = ...` inside a compact constructor is a compile error, not a redundancy, which lesson 0008 turns into a practice item.

Stage 3 produced four more, and the first two are the ones that would have been written wrongly from recall.

- **`Stream.toList` and `Collectors.toUnmodifiableList` disagree about null.** The first accepts a null element, the second throws. Both produce an unmodifiable list, so the names give no hint. Taught as a contrast rather than a rule.
- **The documented "`Files.lines` must be closed" leak does not reproduce the obvious way.** A tight loop reusing one variable survived a hundred thousand iterations, because the cleaner closes unreachable streams. It only failed once every stream was kept reachable, at roughly sixty thousand open descriptors. Lesson 0019 teaches the rule and the mechanism that hides it, since "it works in my loop" is exactly how this reaches production.
- **`String.valueOf(null)` is not a compile-time ambiguity.** It compiles, resolves to the more specific `valueOf(char[])` overload, and throws at run time. The spec for the lesson said "ambiguity" and was wrong; the lesson teaches what happens.
- **A single expression using `+` on strings already compiles to one `StringConcatFactory` call**, checked with `javap`, not to a chain of `StringBuilder` appends. So the familiar advice applies to the loop and not to the expression, and lesson 0021 says which.

Two smaller ones worth not losing: `peek` can be skipped entirely when the terminal operation does not need the elements, which makes it a debugging tool and not a processing step, and end-of-month clamping does not remember the original day, so January the 31st plus two months is the 28th of March rather than the 31st.

Stage 4 produced the largest set, and the first two are the reason this stage was flagged as the hardest to keep honest.

- **`synchronized` no longer pins a carrier thread.** Measured against a genuine two-worker platform pool as a control, virtual threads holding a monitor across a blocking sleep finished in about half a second where the pinned control took about twelve and a half. `Object.wait` inside `synchronized` behaves the same way. JEP 491 made this change in JDK 24. **JEP 444's own text still says `synchronized` pins**, and so does almost everything else written on the subject, including its corollary advice to move off `synchronized` onto `ReentrantLock` for this reason. That advice is now stale, and lesson 0027 says so with the measurement behind it. A real blocking native call, made through the Foreign Function and Memory API, does still pin, and matched the control exactly.
- **The diagnostic built for pinning does not catch the remaining case.** The `jdk.VirtualThreadPinned` flight-recorder event reported zero for the native call that demonstrably pinned, because the event fires on a *failed unmount attempt* and a plain native call never attempts one. It correctly reported zero for the `synchronized` cases too, so the event agrees with the timing in three cases out of three and is useless in exactly the one that matters. This is not spelled out in the JEP and only surfaced from cross-checking event counts against timings. `-Djdk.tracePinnedThreads` produces no output at all now, which is intentional.
- **Structured concurrency is still a preview in this release, and its shape has changed.** `StructuredTaskScope` is now an interface with a static `open()` factory taking a `Joiner`, and two type parameters. The constructor-based form that most write-ups show does not compile even with the preview flag. The lesson shows the verified current shape once, states that it is preview, and tells the reader what to use meanwhile.
- **Unsafe publication of a non-`final` field did not reproduce**, in six million attempts across three runs. Lesson 0023 reports that rather than asserting the failure, and uses it as the deliberate counterpoint to the reordering that did reproduce: a clean run proves nothing, a dirty run proves everything.
- **The naive check-then-act race across two lock acquisitions did not reproduce** with no delay between the check and the act. It reproduced on five runs out of five once the window was widened. Lesson 0024 teaches the widened version and says why the narrow one passed.
- **`findDeadlockedThreads` does not find cycles involving virtual threads.** That is documented rather than measured, and it directly bounds the diagnostic technique lesson 0028 recommends, so both lessons state it.

Stage 5 produced the most consequential drift in the arc so far, and unusually the drift was in the ecosystem rather than in the language.

- **JUnit is at 6, not 5.** The arc's own stage table said "JUnit 5" and `RESOURCES.md` linked the JUnit 5 user guide; both are corrected. JUnit 6 collapsed the three version numbers into one, so the Platform, Jupiter and Vintage all release together, which retires the 1.x-against-5.x explanation that most existing writing on the subject is organised around. The baseline is Java 17 at run time. **The package names did not change**, which is exactly why this is dangerous: Jupiter 5 code compiles unchanged, so nothing forces the author of a lesson to notice. Confirmed by resolving the tree rather than by reading the release note, since `junit-platform-commons` coming out at 6.1.3 is the proof.
- **Surefire injects a test engine that the project never declared.** A project depending only on `junit-jupiter-api`, with no engine, builds and passes under Maven, because Surefire resolves an engine and a launcher onto the provider classpath unasked. The Platform-level failure the three-layer model predicts, `Cannot create Launcher without at least one TestEngine`, only appears when Maven is taken out of the path. Lesson 0029 teaches both, because the convenience is what stops a reader ever learning the model.
- **Maven's own introspection tool disagrees with Maven.** `mvn help:describe -Dcmd=test` reports the generic default plugin version for the packaging, not the version the project actually uses, while `help:effective-pom` and the build log report the real one. Anyone answering "what is bound to this phase" with the obvious command gets an answer about a different project. Taught as a trap in 0034.
- **`@CsvSource`'s comment character applies only to the text-block form.** A row beginning `#` is dropped silently from `textBlock`, so the case count is the only evidence it ever existed, while the array form does not treat `#` as a comment at all. The agent writing 0031 expected uniform behaviour and found the split by running it.
- **`maven-dependency-plugin` 3.7.0 cannot read JDK 25 class files**, failing `dependency:analyze` with `Unsupported class file major version 69`. Pinning 3.11.0 fixes it. This is a plain tooling lag, but it blocks a goal lesson 0033 teaches, so the lesson carries the caution.
- **Nearest-wins mediation is not a depth rule alone.** The conflict built for 0033 turned out to be a tie at equal depth, resolved by declaration order, and the lesson says so precisely instead of dressing it up as a depth story. Preserve that precision if the lesson is ever revised, because the tie-break is the half people get wrong.

Two smaller ones worth not losing: an assertion mismatch counts as a Failure while an uncaught exception counts as an Error, and a failed implicit argument conversion in a parameterised test is therefore an Error rather than a Failure. And `assertTimeoutPreemptively` really does run the body on another thread, observed as `junit-timeout-thread-1` against the test's own thread, which matters for anything thread-confined.

Numbers worth keeping for comparison when this stage is revised, all from one machine and useful as ratios: the platform-thread ceiling landed at 4,066 and 4,068 in two independently written lessons, a million virtual threads were created and blocked in under a second, blocking I/O across virtual threads beat a platform pool by about seventeen times while CPU-bound work showed no gain at all, `LongAdder` beat `AtomicLong` by ten to seventeen times under contention, and holding a lock across a simulated I/O call cost about eight times the throughput of releasing it first.

Stage 6 was the stage where the tools themselves were the drift. Three of the canonical instruments for this subject behave differently on JDK 25 from how every write-up describes them, and one of them does not work at all.

- **The canonical JMH setup produces a jar containing zero benchmarks, silently.** Declaring `jmh-generator-annprocess` as a `provided` dependency, which is what the project's own archetype and every tutorial do, compiles without error or warning and then fails at run time with `Unable to find the resource: /META-INF/BenchmarkList`. Isolated with `javac` directly: on JDK 25 a processor found on the classpath is not run and nothing is said, while `-proc:full` runs it and produces the resource. The fix is `annotationProcessorPaths` on the compiler plugin, which makes processing explicit. This is the single most valuable thing the stage found, because the failure mode is silence.
- **Dead code elimination and constant folding did not reproduce.** These are the two headline effects of the JMH samples, and both were tested properly, at two forks with five two-second iterations. Discarding `Math.log(x)` cost the same as returning it, 6.795 against 6.876 ns/op, both more than ten times the 0.591 empty-method floor. A loop bounded by a `static final` constant cost the same as one bounded by a field, 17.700 against 17.795. Stage 6 therefore teaches the principle, which is that you cannot predict whether the compiler removes work whose result you discard and must consume results rather than hope, and it does not repeat the folklore as though it had been observed. Do not "fix" this if a later reader finds the samples say otherwise: run it again and see.
- **A confirmation run reversed a conclusion, on identical code.** The workload comparison first ran with one fork and reported 1761.6 ± 500.0 against 1679.2 ± 88.1 us/op, from which the honest reading is that precompiling the pattern changed nothing. Re-run with three forks it reported 1880.3 ± 110.6 against 1662.7 ± 19.2, intervals that do not overlap, so the honest reading became a real thirteen per cent. The spec had already been written with the first conclusion and had to be corrected. This is the stage's own lesson happening to the stage, and lesson 41 teaches it from these numbers.
- **JOL does not work on JDK 25 and is no longer worth recommending.** Release 0.17 cannot self-attach: it prints `Unable to get Instrumentation. Dynamic Attach failed` and fails to complete. With `-Djdk.attach.allowAttachSelf=true` the agent loads, the JVM warns that dynamic agent loading will be disallowed by default in a future release, and it still fails to complete. With `-javaagent:jol-core-0.17.jar` the JVM refuses to start, because `jol-core` has no `Premain-Class`. Every article on object layout uses JOL, so the stage carries a one-sentence caution and measures object sizes with the allocation profiler's bytes per operation instead, which needs no agent, no dependency and no flag.
- **Compact object headers are a product flag in JDK 25, not an experimental one, and they are off.** Verified as `{product lp64_product}` defaulting to `false` and accepted with no unlock flag, which is JEP 519. The effect is large and exactly measurable: a `record Point(int x, int y)`, holding 8 bytes of data, allocates 24 bytes by default and 16 with the flag. The default small object is two thirds overhead.
- **Non-generational ZGC is gone and says so at run time.** Passing the old flag produces `Ignoring option ZGenerational; support was removed in 24.0` and then runs anyway, which is JEP 490. Lesson 39 uses this as its live example of why a flag found in a blog post has to be checked against the release in hand.
- **The profile's loudest frame was not the best thing to fix.** Flight Recorder named the regular expression engine, `Pattern.compile` among it, so the obvious fix is to compile once instead of per line. That is real and small: about 1.13 times faster, 2.15 times less garbage. Abandoning the regular expression is about 142 times faster and allocates 40 bytes instead of 2.28 MB. Fixing precisely the frame the profile named captured roughly a thirteenth of the available win, because the frames beneath it were the actual cost. This is why the stage's final lesson is called From Profile to Proof rather than From Profile to Fix.
- **The garbage collection log's verdict was that the collector was not the problem**, which is the verdict a log usually delivers. Sixty young collections, no full collection, 44.2 ms of total pause against a 7441 ms run, so 0.6 per cent of wall-clock time. Lesson 40 teaches computing that fraction before touching a flag, precisely because the reflex is to tune the collector.

Two smaller ones. JMH 1.37 on JDK 25 prints the JEP 498 warning, `A terminally deprecated method in sun.misc.Unsafe has been called`, naming `objectFieldOffset` called from `org.openjdk.jmh.util.Utils`: expected noise from the harness, not a defect in the reader's benchmark, and worth saying so because it looks alarming. And JMH formats scores with the platform's default locale, so in a comma-decimal locale `86.412 ns/op` prints as `86,412 ns/op` and reads as eighty-six thousand; pass `-Duser.language=en -Duser.country=US` before sharing numbers.

Stage 7 was the stage where the shared spec itself was wrong three times and the lesson authors caught it, which is the strongest evidence so far that assigning verification to the writer rather than to the designer is the right division of labour. Every correction below came from an agent running the thing rather than accepting what it was handed.

- **`jdeprscan` reports only uses of Java SE's own deprecated API, never your project's or a third party's.** The spec had recorded a symptom of this as a `--class-path` invocation trap, which was a misdiagnosis: no invocation reports a project's own deprecations, because JEP 277 says the default set is the deprecations from Java SE itself. Verified both ways, with a caller of a locally deprecated method reporting nothing and a caller of `new Integer(3)` reporting. The consequence is the fact worth keeping: `jdeprscan` answers "will the platform break this code", and only the compiler's `-Xlint:deprecation` answers "is anyone still calling the thing we deprecated". They are not alternatives.
- **A removal commitment can be withdrawn, not merely postponed.** Asking `jdeprscan --list` for each release shows `java.lang.Integer(int)` annotated `@Deprecated(since="9")` at release 11, `@Deprecated(since="9", forRemoval=true)` at 17 and at 21, and back to `@Deprecated(since="9")` at 25. Independently confirmed. This is a sharper argument than the one the spec set out to make and lesson 0046 is built on it: if the platform can un-commit to removing a constructor deprecated since 2017, an internal "deprecated" with no date will certainly not be removed.
- **The JEP staleness trap is narrower and more interesting than stage 4 recorded it.** JEP 444 and JEP 491 **are** cross-linked, each carrying a "Relates to" line naming the other in its header table. What was never revised is JEP 444's prose, which still describes pinning inside `synchronized` as current and merely speculates that a future release may remove it. So a reader who follows the header link is fine and a reader who reads the body is misled, and a header link is not a correction. JEP 491 is delivered in Java 24, and a virtual thread blocking inside `synchronized` on JDK 25 produced no trace under `-Djdk.tracePinnedThreads=full`. Keep this precision if lesson 0047 is revised; the loose version overstates the case and is easy to rebut.
- **Adding a constructor overload only breaks a `null` call site when the new parameter type is unrelated to the old one.** The spec implied it generally. `Widget(String)` beside `Widget(Object)` compiles and resolves `null` to the more specific `String`; only `Widget(String)` beside `Widget(Integer)` produces `reference to Widget is ambiguous`. Lesson 0045 teaches the precise version and made the distinction its hardest practice item.
- **Reordering a record's components corrupts behaviour silently, with no diagnostic at all.** Two components of the same type swapped in the header, an unrecompiled caller, and the values come back transposed: no compile error, no runtime error, just wrong data. This is the most dangerous single change the stage found and it appears in the sheet's silent-failure column.
- **Narrowing a sealed hierarchy's `permits` clause fails loudly and widening it fails quietly.** Removing a permitted subclass throws `IncompatibleClassChangeError: Failed listed permitted subclass check` from the class loader, before any of your code runs. Adding one gives the stage 2 failure instead, `MatchException`, at the moment an unrecompiled exhaustive switch meets the new case. The asymmetry is a teaching point rather than a curiosity: the change that looks more destructive is the one the platform catches for you.
- **Constant inlining is worse than a stale field read.** `javap -c` on a caller of a `public static final int` shows the value folded into the surrounding string literal, `ldc // String MAX      = 10`, so there is no field access left in the class file to re-resolve. A caller cannot see a corrected constant without recompiling, and nothing in the caller's bytecode even refers to the library at that point.
- **Narrowing a return type from `List` to `ArrayList` is source-compatible and binary-incompatible.** The unchanged caller source recompiles cleanly; the unchanged caller class file throws `NoSuchMethodError: 'java.util.List lib.Config.items()'`, naming the descriptor that no longer exists. This pair is the clearest demonstration in the arc that the compiler's idea of "the same method" and the class file's idea of it are different.

Two smaller corrections, both mine. The spec described `Integer(int)` as a sixteen-year-old deprecation; Java 9 shipped in 2017, so it is nine years old, and lesson 0046 says nine. And the spec's quoted `javac` output omitted the file and line prefix that real output carries, so the lesson quotes the fuller text.

Stage 7 numbers worth keeping: the platform carried 720 deprecated API items at JDK 25 with 132 of them marked for removal, against 583 and 25 at JDK 11, so deprecations grow slowly and removal commitments grew more than five times across four long-term-support releases while still covering only about eighteen per cent of deprecations. Of the 720, 135 are tagged `since="9"`, the modularisation aftermath, and 26 arrived in 25 itself.

Stage 6 numbers worth keeping, all from one machine and useful only as ratios: escape analysis took an allocation from 24 bytes per operation to effectively zero and the non-escaping version ran about 4.8 times faster; compact object headers took the same object from 24 bytes to 16; `int[5]` measures 40 bytes; the empty-benchmark floor sat at about 0.59 ns/op and four different cheap expressions all landed between 0.589 and 0.646, which is the level at which nothing can be compared.

## Version policy

The baseline is the current long-term-support release, which was JDK 25 when this workspace was prepared. Two rules follow, and they matter more here than in most languages:

- Every language feature is introduced with the release it arrived in. Java is read across many versions, and a lesson that says "modern Java" without a number is useless in three years.
- Before a stage is written, check the JEP index and the release page in `RESOURCES.md` for what has changed. The six-month cadence means the arc will drift on its own.

Recheck which release is long-term-support when resuming this workspace after a gap. The support roadmap in `RESOURCES.md` is the source for that, not recall.

Rechecked against that roadmap before stage 2 was written: 8, 11, 17, 21 and 25 are the long-term-support releases, and the next is 29, planned for September 2027. JDK 25 is therefore still the right baseline and no link needs moving.

Rechecked again before stage 5, as the note above directed. The roadmap still gives the same five long-term-support releases and the same next one, so the baseline did not move. **The warning was right about the drift and wrong about where it would be.** Nothing in the JDK had changed; the testing library had gone up a major version. So widen the rule: before a stage, recheck the release policy of every dependency the stage teaches, not only the platform's. The version numbers stage 5 was verified against are listed in the stage's reference sheet, which is the right place to start from when this is next revisited.

## Rechecked after the arc was finished: the one fact that was expected to expire has not

The arc's own work order recorded that lesson 0037's off-by-default fact about compact object headers "stops being true in JDK 27", and lesson 0037 itself cites JEP 534, Compact Object Headers by Default, as already delivered and targeting that release. **Checked on JDK 26, which is now the newest release available here, one beyond the arc's JDK 25 baseline: the fact still holds.** `-XX:+PrintFlagsFinal` reports

```text
bool UseCompactObjectHeaders                  = false                          {product lp64_product} {default}
bool UseCompressedOops                        = true                           {product lp64_product} {ergonomic}
```

which is exactly what lesson 0037 describes for JDK 25, including the `{product lp64_product}` category that distinguishes it from the JDK 24 experimental flag and the `{ergonomic}` marking on compressed references. **So the lesson needs no change, and a reviser should not "correct" it.** Recording the non-change deliberately, because the danger here is the opposite of the usual one: a note predicting an expiry invites somebody to act on the prediction rather than on a run.

What this does change is the trigger. The expiry is real but has not arrived, so the recheck is: **run that flag query on JDK 27 when it ships**, and only then rewrite the subsection, keeping the JDK 25 and 26 measurements as the history the lesson is arguing from. The long-term-support baseline itself has not moved; 29 remains the next one.

**The related item that is genuinely open is broader than one flag.** Stage 6's findings were all made on JDK 25, and three of them are about tooling rather than the language: JOL 0.17 failing to self-attach, the JMH setup that silently produces a jar with no benchmarks, and `maven-dependency-plugin` needing 3.11.0 to read JDK 25 class files. Tooling is where this workspace has drifted every single time, so those three are the ones to re-run against JDK 26, not the language facts.

### All three tooling findings re-run on JDK 26

Done with Maven 3.9.16 on JDK 26.0.2.1, one release past the arc's baseline. **All three claims survive, and the useful correction is to two of them being written more narrowly than the fact deserves.**

- **`maven-dependency-plugin`: the claim holds and generalises, and the default is still the broken version.** A module compiled with `release` 26 produces class file **major version 70**, and Maven still resolves **3.7.0** by default, which fails with `Byte code of 'probe.Main' is corrupt ... Unsupported class file major version 70`. Pinning 3.11.0 reads it and reports both directions correctly, `Used undeclared dependencies found: org.apache.commons:commons-lang3` against `Unused declared dependencies found: org.apache.commons:commons-configuration2`, which is lesson 0033's own teaching case reproduced a release later. **So the number in that message names the release you compiled for rather than a bug somebody fixed**, and the lesson and the sheet now say so instead of quoting only 69.
- **The JMH annotation-processor trap is unchanged, including its silence.** Declaring `jmh-generator-annprocess` at `provided` scope on JDK 26 builds successfully, warns about nothing at all, and leaves no `META-INF/BenchmarkList` in the output, so the packaged jar still contains zero benchmarks. Adding `annotationProcessorPaths` to the compiler plugin generates it. Both halves verified, so the lesson needed no change beyond the sheet recording that the recheck happened.
- **JOL 0.17 is worse than "does not complete", and the sheet now says how each method fails.** Three methods, three different failures on JDK 26. Plain self-attach prints `Unable to get Instrumentation. Dynamic Attach failed`, then terminal-deprecation warnings naming `sun.misc.Unsafe::arrayBaseOffset` called from JOL's own `HotspotUnsafe`, then throws `Cannot get the field offset, try with -Djol.magicFieldOffset=true`, **and that suggested flag does not rescue it either**. That throw is not even stable: under a watchdog it threw within twenty seconds, and two later runs without one hung instead, one for ten minutes and one for fifteen, so the plain method sometimes fails and sometimes never returns. With `-Djdk.attach.allowAttachSelf` it attaches, warns that "Dynamic loading of agents will be disallowed by default in a future release", and then **produces no layout at all**, so it hangs rather than failing. As `-javaagent` the virtual machine refuses to start: `Failed to find Premain-Class manifest attribute`, `agent library failed Agent_OnLoad: instrument`. The recommendation against it stands and is now better evidenced.

**The pattern across all three is worth keeping.** None of the three was wrong, and two were written as facts about JDK 25 when they are really facts about a moving target: a plugin's default staying old while the class file version advances, and a tool depending on agent attachment that the platform is withdrawing. **A tooling finding should be stated as the mechanism plus the instance, not the instance alone**, or every recheck reads as though the claim expired when only its number did.

## On the arc

- Resolved: stage 2 did depart from how Java is usually taught, and the departure held. Records (0008) and interfaces (0009) come before `extends` (0010), and by the time inheritance arrives the reader has already modelled two domains without it, so lesson 0010 can price it rather than sell it. Sealed types then land as a restriction on something already understood. Keep this order.
- Resolved: stage 4 was the hardest to keep honest, and the split the note predicted held. "Java Concurrency in Practice" carried the model, meaning happens-before, publication and the failure modes, and none of that has aged. Every API claim came from a run instead, and that is what caught the stale pinning advice. The warning to watch for a 2006 idiom taught as current turned out to understate the problem: the stalest advice came from a 2023 JEP rather than a 2006 book.
- Resolved: the build tool question, which stood open through four stages, is answered in the State section above. Maven, 3.9 or later.
- Resolved: stage 6's workload is a **log-line indexer the reader builds**, and the reasoning the open thread set out is the reasoning that decided it. Nothing is added to this repository. The reader builds one Maven project with the stage 5 machinery, meaning declared dependencies, `mvn package` and a runnable jar with a `Main-Class`, so the stage introduces no build machinery of its own and stage 5 gets used rather than remembered. The program generates its own synthetic log lines, so there is no dataset to download and no non-determinism from someone else's data. Each round parses every line, counts by severity, and keeps the longest few messages per severity; the whole pass repeats for a configurable number of rounds, because a profile needs a program that runs for tens of seconds rather than milliseconds. It is written first in the obvious way, which allocates heavily, and each of those allocations is a real thing a profile can find and a benchmark can then prove was worth fixing. The constraint this satisfies is that the workload needs no dependency, no network, no dataset and no particular hardware, so any reader can run it and see the same shape of result even though the numbers will differ.

- Resolved: asking the sheet writer explicitly for contradictions, rather than only for a sheet, paid for itself again in stage 6 and should stay in the method. It is the only reader that sees the whole stage at once. It found a word defined two incompatible ways across the stage, "throughput", meaning the share of machine time not spent collecting in lesson 39 and a rate of operations per second in lesson 41, both standard in their own subject and neither flagged; lesson 41 now names the collision. It also found a sentence in lesson 38 comparing a duration against a measurement lesson 37 had made in bytes, which was incoherent and had the ratio inverted as well. One of its three findings was wrong, and worth recording as a caution: it judged lesson 37's citation of JEP 534 for the header bit-width to be a citation swap, on the reasonable assumption that JEP 534 only flips the default. Fetching JEP 534 showed it states the 96-bit to 64-bit change itself, so the lesson was right and the objection was inference. Check a reported contradiction against the source before acting on it.

- Resolved, and this one is about the publishing pipeline rather than about Java, so the durable form of it is now in the skill's `PUBLISHING.md`. Lesson 0044 shipped with five of its seven collapsible blocks rendering as raw tags with their markdown unparsed, and every source-level check passed: the openers sat at column zero, the blank lines were right, the strict build was clean, and the checker was happy. The cause is that `<` immediately followed by `?` reads as the start of a processing instruction to Python-Markdown's HTML block parser, which runs before inline code is protected, so a single `` `Collection<?>` `` inside a block stops `md_in_html` for that block and every later block in the file. Java generics make this a standing hazard here rather than a curiosity. Two things came out of it: the fix, which is `<code>List&lt;?&gt;</code>` and renders identically, and the realisation that the checker had been proving the source looked right rather than proving the page rendered. It now renders each lesson with the site's own extensions and fails if any block arrives unprocessed, which immediately found a second, latent instance in lesson 0013 that was harmless only because it was the last block in that file.
- Resolved: the sheet writer's contradiction hunt earned its place for the second stage running, and its yield changes what to ask it. In stage 7 it found that lesson 0047 routed binary-compatibility questions to the virtual machine specification while lesson 0045 rested on the language specification's chapter 13, which is titled Binary Compatibility: both defensible, jointly confusing, and now reconciled in 0047 by separating the rule from the mechanism. It found lesson 0047 re-deriving the `MatchException` failure that lesson 0045 had already taught, without crediting it, so 0047 now takes the failure as given and contributes only the specification citation and the bytecode. And it found a real gap rather than a contradiction: the stage taught two deprecation warning categories without ever saying how to suppress either, so lesson 0046 now covers the two `@SuppressWarnings` keys, verified on JDK 25 as silencing only their own category. Ask the sheet writer for gaps as well as contradictions next time; it found one unprompted.

## Open threads

- Resolved, partly: the gap was recorded as garbage-collection tuning having no book-length source, and searching for one showed the gap was really two. How collectors work does have a book-length source, "The Garbage Collection Handbook" in its second edition, which is now listed. How to tune a specific collector on a specific release still does not, and the honest answer is that it should not: the primary source is the HotSpot garbage-collection tuning guide for the release in hand, which is versioned and is now listed for JDK 25. Do not go looking for a tuning book again. Record instead that tuning advice ages faster than books ship, which is the same reason this workspace already refuses to source version-sensitive claims from books.
- Resolved, and like the stage 6 gap it was really two. There is **no authoritative source for the verdict**, and there should not be: whether Spring earns its place in a particular service is a judgment about that team, that service and its constraints, not a technical fact anyone can publish, and any source claiming to settle it in general is selling something. There **is** a source for the reasoning, now listed: "Choose Boring Technology", which gives the cost of novelty a shape a reader can argue with, and Fowler's "Yagni" for the half of the problem that is speculative generality. Lesson 0049 therefore teaches a rubric the reader applies to a candidate, and it deliberately names no winner. Do not go looking for a framework-comparison source again, and do not let a later revision turn that lesson into a Spring assessment.
- Concurrency comparisons across workspaces are tempting here: virtual threads against goroutines, against `asyncio`. Keep them as pointers to the relevant glossary term in the other workspace rather than importing its material.
